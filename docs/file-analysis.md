# File Analysis: `src/` Directory

Generated on 2026-06-02. Describes the purpose and contents of every file in `src/`.

---

## Root Files (`src/`)

| File                   | Description                                                                                                                                                                                                                                           |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/router.tsx`       | TanStack Router factory. Creates the app router with TanStack Query SSR integration, default catch boundary, 404 component, scroll restoration, and intent-based preloading. Also augments `@tanstack/react-router` module for type-safe route usage. |
| `src/routeTree.gen.ts` | Auto-generated route tree by TanStack Router. Imports all route modules, assembles the hierarchical route tree, and exports typed interfaces (`FileRoutesByFullPath`, `FileRoutesByTo`, etc.) plus the `routeTree` object. **Do not edit manually.**  |
| `src/vite-env.d.ts`    | Vite environment type declarations. Defines `import.meta.env` with `VITE_BASE_URL` and `VITE_CLOUDFLARE_R2_PUBLIC_URL` variables, and the `ImportMeta` interface.                                                                                     |
| `src/styles/app.css`   | Global CSS entry point. Single `@import "tailwindcss"` line to make all Tailwind CSS v4 utility classes available.                                                                                                                                    |
| `src/utils/seo.ts`     | SEO meta tag generator. The `seo()` function accepts title, description, keywords, and image to return an array of tag objects (standard meta, Twitter Card, Open Graph) for `HeadContent`.                                                           |

---

## `src/db/`

**Migrations and metadata generated by Drizzle Kit.**

| File                                          | Description                                                                                                                                                |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/db/drizzle/0000_wandering_jimmy_woo.sql` | Initial migration SQL. Creates `users`, `posts`, `comments`, `tags`, and `post_tags` tables with foreign key constraints.                                  |
| `src/db/drizzle/meta/0000_snapshot.json`      | Drizzle Kit schema snapshot for migration 0000. JSON representation of all tables, columns, types, foreign keys, and indexes. Used for schema diffing.     |
| `src/db/drizzle/meta/_journal.json`           | Drizzle Kit migration journal. Records migration history (currently a single entry for `0000`) with timestamps and tags. Used to track applied migrations. |

---

## `src/components/`

### UI Primitives (`src/components/ui/`)

| File                 | Description                                                                                                                                                                                  |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `color-mode.tsx`     | Chakra UI color mode provider and utilities. Wraps `next-themes`' `ThemeProvider`, exports `useColorMode`, `useColorModeValue`, `ColorModeIcon`, and `ColorModeButton` (toggle button).      |
| `password-input.tsx` | Password input with visibility toggle. Renders an `InputGroup` with an eye icon button to show/hide the password text.                                                                       |
| `provider.tsx`       | Root UI provider. Composes `ChakraProvider` with `ColorModeProvider` using `defaultSystem`.                                                                                                  |
| `tag-input.tsx`      | Tag selection/creation combobox. Allows selecting existing tags (fetched via query) or creating new ones, rendered as removable `Badge` elements.                                            |
| `toaster.tsx`        | Chakra UI toast notification system. Renders a `ChakraToaster` in a portal with indicators, descriptions, and action buttons. Exports `Toaster` component and imperative `toaster` instance. |
| `tooltip.tsx`        | Chakra UI tooltip wrapper. Renders `Tooltip.Root` with optional arrow and portal, or just children when `disabled` is true.                                                                  |

### Form Components (`src/components/form/`)

| File            | Description                                                                                                                                                            |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `FieldInfo.tsx` | Form field error display for TanStack Form. Shows error messages in red when touched and invalid, or "Validating..." during validation.                                |
| `FieldText.tsx` | Reusable text/textarea field wrapper for Chakra UI + TanStack Form. Renders label, required indicator, helper text, `Input` or `Textarea`, and `FieldInfo` for errors. |

### Post Detail Components (`src/components/PostDetail/`)

| File                    | Description                                                                                                                                             |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PostDetailDisplay.tsx` | Read-only post detail view. Renders a back button, the `Post` component in a bordered box, and the `Comments` section below.                            |
| `PostEditForm.tsx`      | Post edit form. Uses TanStack Form with title/textarea fields, `TagInput`, and save/cancel buttons. Uses `useBlocker` to prevent navigation when dirty. |

### Main Components (`src/components/`)

| File                          | Description                                                                                                                                                                                                          |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Comments.tsx`                | Comment system for posts. Renders a comment list with a submit textarea, each comment showing content, timestamp, and a delete button (with confirmation dialog). Uses suspense-fetched comments and mutation hooks. |
| `DefaultCatchBoundary.tsx`    | Top-level error boundary for TanStack Router. Renders an error display with "Try Again" and "Go Back"/"Home" buttons.                                                                                                |
| `GlobalShortcuts.tsx`         | Global keyboard shortcuts handler. Shows a `?` icon button (bottom-left) that opens the keyboard shortcuts dialog, plus hotkey bindings for `Shift+/`, `G+P`, `G+U`, `G+S` navigation sequences.                     |
| `KeyboardShortcutsDialog.tsx` | Modal dialog listing keyboard shortcuts. Displays key sequences as `Badge` elements in a `Table`, covering navigation and video frame seeking.                                                                       |
| `NotFound.tsx`                | 404 fallback component. Shows a message, "Go back" button, and "Start Over" link to `/`.                                                                                                                             |
| `Pagination.tsx`              | Pagination control. Renders previous/next chevron buttons, page number buttons with ellipsis gaps, and highlights the current page.                                                                                  |
| `PopularTagsSection.tsx`      | Popular tags display. Shows a heading "Popular Tags" followed by `Badge` links (with post counts) to `/posts/tags/$tag`.                                                                                             |
| `Post.tsx`                    | Full post detail display. Renders video player (if `videoKey`), title, content, tag list, related post link, author card, and an edit button for the post owner.                                                     |
| `PostCard.tsx`                | Post card for list/grid views. Renders a 16:9 thumbnail image, title (2-line clamp), and content preview (1-line clamp) as a `Link` with hover effects.                                                              |
| `PostError.tsx`               | Post-specific error boundary. Delegates to `ErrorComponent` from TanStack Router.                                                                                                                                    |
| `PostFilters.tsx`             | Post filter bar. "Sort By" badges (Newest/Oldest) and "Date Range" badges (All Time, Last 7/30/90/365 days) that update URL search params via `useNavigate`.                                                         |
| `PostsPageLayout.tsx`         | Main layout for posts pages. Responsive grid with sidebar (search box, filters, popular tags) and main content area. Shows video metadata on post detail routes.                                                     |
| `SearchBox.tsx`               | Search input with tag combo-box. Renders a text input, tag selector with removable badges, and a search button. Uses debounced navigation.                                                                           |
| `User.tsx`                    | User profile card/link. Renders an `Avatar` and name inside a `Link` to `/users/$id`.                                                                                                                                |
| `UserError.tsx`               | User-specific error boundary. Delegates to `ErrorComponent` from TanStack Router.                                                                                                                                    |
| `Video.tsx`                   | Video player using `media-chrome`. Renders a `MediaController` with full custom controls (play/pause, mute, volume, seek, time display, fullscreen, playback rate) and frame-by-frame hotkeys.                       |

---

## `src/lib/`

### `src/lib/auth/`

| File                      | Description                                                                                                                                                                                                            |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `auth.hooks.ts`           | React Query mutation hooks wrapping Better Auth. Exports `useLogin`, `useSignUp`, `useUpdateProfile`, `useChangePassword`, `useDeleteAccount`, and `useSocialLogin` with query invalidation and navigation on success. |
| `auth.hooks.test.tsx`     | Tests for auth React Query hooks. Mocks Better Auth client, router, and toaster to verify correct client method calls and navigation/invalidation on success.                                                          |
| `auth.middleware.ts`      | Server-side auth middleware using Effect. Defines `AuthRequiredError`, `getUserSessionEffect` (returns user or null), and `requireAuthEffect` (returns user or fails). Wraps them as TanStack server functions.        |
| `auth.middleware.test.ts` | Tests for server-side auth middleware effects. Mocks `AuthService` and `RequestHeadersService` to verify session handling, null user return, and auth-required failure behavior.                                       |
| `auth.schemas.ts`         | Zod validation schemas for auth forms: `loginSchema`, `signUpSchema` (with password match refinement), `profileSchema`, and `passwordSchema`.                                                                          |
| `auth.schemas.test.ts`    | Tests for auth Zod schemas. Validates correct data acceptance and error cases (invalid emails, short passwords, password mismatch, etc.).                                                                              |
| `client-context.tsx`      | React Context provider for the Better Auth client instance. Enables auth hooks to access the client without prop drilling.                                                                                             |
| `client.ts`               | Better Auth client initialization for the browser. Creates the singleton `authClient` via `createAuthClient` with the base URL from `VITE_BASE_URL`.                                                                   |
| `context.ts`              | Effect service definitions for server-side auth dependencies: `AuthSessionProvider`, `AuthService`, and `RequestHeadersService`.                                                                                       |
| `index.ts`                | Server-side Better Auth initialization. Creates the `auth` instance with Drizzle ORM PostgreSQL adapter, email/password auth, TanStack Start cookies plugin, and session caching.                                      |

### `src/lib/assets/`

| File     | Description                                                                                                                                                 |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `url.ts` | Asset URL builder. Exports `assetUrl(key)` which joins an R2 object key with `VITE_CLOUDFLARE_R2_PUBLIC_URL` to generate fully qualified public asset URLs. |

### `src/lib/comments/`

| File                      | Description                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `comments.service.ts`     | Comments domain service and server functions. Defines `CommentsService` (Effect `Context.Service`), `CommentsServiceLive` (layer), Effect wrappers (`fetchCommentsEffect`, `addCommentEffect`, `deleteCommentEffect`), and TanStack Start server functions (`fetchComments`, `addComment`, `deleteComment`). Auth checks and ownership enforcement are inlined here. |
| `comments.fn.test.ts`     | Integration tests for comment server functions. Sets up in-memory Kysely database and tests fetch/add/delete with auth checks, ownership validation, and error handling.                                                                                                                                                                                             |
| `comments.fn-context.tsx` | React Context holding default comment server function implementations. Enables dependency injection and testing by swapping implementations.                                                                                                                                                                                                                         |
| `comments.hooks.ts`       | React Query mutation hooks for comments. Exports `useAddComment` and `useDeleteComment` with toast notifications and automatic query invalidation.                                                                                                                                                                                                                   |
| `comments.hooks.test.tsx` | Tests for comment mutation hooks. Verifies correct payloads, query invalidation on success, and error toasts on failure.                                                                                                                                                                                                                                             |
| `comments.queries.ts`     | TanStack Query key factories and `queryOptions` for comments. Defines `commentsKeys` and `commentsQueryGetComments` (by post ID).                                                                                                                                                                                                                                    |
| `comments.schema.ts`      | Zod validation schema for comment objects. Exports `commentsSelectSchema` and `commentInsertSchema`.                                                                                                                                                                                                                                                                 |

### `src/lib/db/`

| File                        | Description                                                                                                                                                                                                                   |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `collections.ts`            | TanStack DB reactive collections. Defines `tagsCollection` and `usersCollection` (eager sync from server), plus `commentDraftsCollection` and `uploadDraftCollection` (localStorage persistence).                             |
| `context.ts`                | `KyselyDB` Effect service definition. A typed, dependency-injectable reference to the Kysely database layer via Effect's `Context.Service`.                                                                                   |
| `e2e-db.ts`                 | E2E test database factory. Creates a PGlite in-memory instance, runs Drizzle migrations against it, and returns a Kysely instance with `PGliteDialect`.                                                                       |
| `kysely.ts`                 | Central Kysely setup. Converts Drizzle schema definitions to Kysely-compatible types via `Kyselify` and creates the singleton `kysely` instance with a Postgres dialect.                                                      |
| `layer-factories.server.ts` | Async Effect Layer factories for server use. `makeDBLayer`, `makeAuthLayer`, and `makeMiddlewareLayer` dynamically import dependencies and construct layers providing `KyselyDB`, `AuthService`, and `RequestHeadersService`. |
| `pglite-driver.ts`          | Custom Kysely dialect backed by `@electric-sql/pglite` (in-memory Postgres). Translates Kysely queries into PGlite's native interface for testing/local dev.                                                                  |
| `pool.ts`                   | Database connection pooling. Creates a Neon Serverless `Pool` singleton, exports a Drizzle instance using Neon HTTP client, and provides `getKyselyPool()`.                                                                   |
| `test-utils.ts`             | Testing utilities for integration tests. Includes full schema DDL string, `runSchema` function to apply it, and `createTestKysely` factory returning an in-memory PGlite-backed Kysely instance.                              |

### `src/lib/db/schema/`

| File               | Description                                                                                                                                                                      |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `auth.schema.ts`   | Drizzle ORM schema for Better Auth tables: `user`, `session`, `account`, and `verification`. Also generates Zod insert/select schemas for the `user` table.                      |
| `sakuga.schema.ts` | Drizzle ORM schema for application-domain tables: `tags`, `posts`, `postTags` (junction), and `comments`. Also defines `relations` (one-to-many, many-to-one, self-referencing). |
| `sakuga.utils.ts`  | Generates Zod insert/select schemas for `tags`, `posts`, and `comments` via `drizzle-zod`. Exports `DbSchemaSelect` and `DbSchemaInsert` aggregated types.                       |
| `index.ts`         | Barrel file re-exporting all schema exports from `auth.schema`, `sakuga.schema`, and `sakuga.utils`.                                                                             |
| `seed.ts`          | Database seeding script. Uses `drizzle-seed` to populate the `posts` table with 150 rows. Runs only when `SEED_DB=true`.                                                         |

### `src/lib/effect/`

| File              | Description                                                                                                                                                                                                                                  |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `effect.utils.ts` | Effect wrapper around Kysely (`EffectKysely<DB>`). Adapts raw Kysely queries into `Effect.Effect`, provides `executeTakeFirstOption`, `executeTakeFirstOrError`, and a generator-based `transaction()` API. Defines `SqlError` tagged error. |
| `logger.ts`       | Effect logging utility. Exports `withMinimumLogLevel` layer-composer and typed log level constants (`All`, `Fatal`, `Error`, `Warn`, `Info`, `Debug`, `Trace`, `None`).                                                                      |
| `tracing.ts`      | OpenTelemetry tracing integration. Exports `TracingLive` layer using `@effect/opentelemetry` `NodeSdk` with OTLP trace and log exporters for production observability.                                                                       |

### `src/lib/env/`

| File        | Description                                                                                                                                                                                                   |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `client.ts` | Client-side environment validation. Loads and validates `import.meta.env` variables (`VITE_BASE_URL`, etc.) using Effect's `Config`, validates `MODE` is `"development"` or `"production"`.                   |
| `server.ts` | Server-side environment validation. Loads and validates `process.env` variables (`DATABASE_URL`, `BETTER_AUTH_SECRET`, R2/GitHub OAuth keys, etc.), validates `BETTER_AUTH_SECRET` min length and `NODE_ENV`. |

### `src/lib/posts/`

| File                   | Description                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `posts.service.ts`     | `PostsService` Effect service and server functions. Core business logic for `search` (tag/date/sort/paginate via Kysely), `fetchDetail` (with related post and comments), `upload` (R2 multipart with media analysis and thumbnail generation), `getByTag`, and `update`. Server functions (`searchPosts`, `fetchPostDetail`, `uploadPost`, `updatePost`, `getPostsByTag`) are embedded at the bottom of this file. |
| `posts.fn.test.ts`     | Integration tests for post server functions. Seeds test data and verifies search/filter/pagination, detail fetching with related posts, tag filtering, and update authorization.                                                                                                                                                                                                                                    |
| `posts.hooks.ts`       | React hooks for posts. Exports `usePostsPage` which manages pagination with placeholder data, URL search param updates, scroll-to-top, and extracts `posts`, `popularTags`, and `isFetching`.                                                                                                                                                                                                                       |
| `posts.queries.ts`     | TanStack Query `queryOptions` factories for posts: `byTag`, `detail`, `search`. Defines `postsKeys` for cache management and exports convenience functions.                                                                                                                                                                                                                                                         |
| `posts.schema.ts`      | Zod schemas for post data: `VideoMetadataSchema` (codec, frame rate, resolution), `FormFileUploadSchema`, `updatePostInputSchema`, `searchPostsBaseSchema`, and `postByTagSchema`.                                                                                                                                                                                                                                  |
| `posts.schema.test.ts` | Tests for posts Zod schemas. Validates defaults, correct inputs, and error cases (negative pages, invalid sort options, unknown keys).                                                                                                                                                                                                                                                                              |
| `posts.utils.ts`       | Utility functions for posts: `filterAndSortPosts`, `mapPopularTags`, and `ALLOWED_VIDEO_EXTENSIONS` constant.                                                                                                                                                                                                                                                                                                       |

### `src/lib/tags/`

| File              | Description                                                                                                                                                                                                                                               |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tags.service.ts` | `TagsService` Effect service and server functions. Implements `all` (fetch all tags) and `popular` (top 10 tags by post count via join). Server functions (`getAllTags`, `getAllPopularTags`) are embedded in this file. Exports `TagsServiceLive` layer. |
| `tags.fn.test.ts` | Integration tests for tag server functions. Tests empty/all tags retrieval and popular tags with correct post counts after seeding.                                                                                                                       |
| `tags.queries.ts` | TanStack Query `queryOptions` factories for tags: `getTags` and `getPopularTags`. Defines `tagsKeys` and exports convenience functions.                                                                                                                   |
| `tags.utils.ts`   | Tag utility functions: `mapPopularTags` converts DB results (with bigint counts) to plain `{ id, name, postCount }` objects.                                                                                                                              |

### `src/lib/upload/`

| File                       | Description                                                                                                                                                                                                                                                                                              |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `upload.processor.ts`      | Client-side video file processing utilities. `makeReadChunk` for `mediainfo.js`, `analyzeVideo` for metadata extraction, `generateThumbnails` for frame capture, `generateAutoThumbnails` for 5 evenly-spaced auto thumbnails, and `buildFormData` for serialization.                                    |
| `upload.processor.test.ts` | Tests for upload processor. Tests `buildFormData` serialization (string/File/array/object, undefined skipping) and `makeReadChunk` chunk reading.                                                                                                                                                        |
| `useUploadDraft.ts`        | TanStack DB draft persistence hook. Provides `{ draft, isLoaded, persist, clear }` for auto-saving incomplete upload forms to localStorage.                                                                                                                                                              |
| `useUploadForm.ts`         | Upload form state hook. Manages the full upload workflow: video file, metadata, thumbnails, tags, and builds the FormData payload for submission.                                                                                                                                                        |
| `useVideoProcessing.ts`    | Video processing hook. Manages MediaInfo WASM lifecycle, video file selection, preview URL, thumbnail generation/capture, and frame rate state. Returns `{ videoFile, previewUrl, frameRate, thumbnails, selectedThumbnailIndex, videoMetadata, selectFile, captureFrame, selectThumbnail, clearFile }`. |

### `src/lib/storage/`

| File                | Description                                                                                                                                                                        |
| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `storage.module.ts` | `StorageModule` Effect service definition. Interface with `uploadVideo(userId, file)`, `uploadThumbnail(userId, file)`, `deleteFile(key)`. Tagged error: `StorageError`.           |
| `storage.s3.ts`     | S3/R2 implementation of `StorageModule`. Uses `@aws-sdk/client-s3` with Cloudflare R2 endpoint. Key generation: `{namespace}/{userId}/{uuid}.{ext}`. Provides `StorageLive` layer. |
| `storage.memory.ts` | In-memory test implementation of `StorageModule`. Stores files in a `Map<string, Uint8Array>` for fast integration tests without cloud access.                                     |
| `storage.test.ts`   | Tests for `StorageModule`. Tests uploadVideo, uploadThumbnail, and deleteFile operations using the in-memory implementation. Verifies key formatting and content storage.          |

### `src/lib/pagination/`

| File            | Description                                                                                                                                                                                          |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pagination.ts` | Pagination utility. `computePagination(totalCount, { page, pageSize })` returns `PaginationMeta` with `currentPage` (1-indexed), `totalPages`, `hasMore`, `hasPrevious`, `offset`, `limit`, `total`. |

### `src/lib/` (root)

| File                   | Description                                                                                                                                                                                             |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `errors.ts`            | Effect tagged error classes: `PostNotFoundError`, `UnauthorizedError`, `ForbiddenError`, `CommentNotFoundError`, `UserNotFoundError`, `ValidationError`. Used for type-safe error handling in services. |
| `ids.ts`               | Branded type schemas: `PostId`, `UserId`, `TagId`, `CommentId`. Provides compile-time type safety to prevent mixing entity ID types.                                                                    |
| `polyfills.ts`         | crypto.randomUUID polyfill for environments where `globalThis.crypto.randomUUID` is unavailable (e.g., older Node.js). Uses a fallback UUID v4 generator.                                               |
| `query-client.ts`      | Singleton TanStack Query client. `getQueryClient()` returns a lazily-initialized `QueryClient` with 60s default stale time.                                                                             |
| `sanitize.ts`          | HTML sanitization utility. The `sanitize` function strips `<script>`, `<iframe>`, `<object>`, `<embed>` tags, inline event handlers, and `javascript:` URLs from user input.                            |
| `server-fn.handler.ts` | `createHandler` factory. Bridges TanStack Start server functions with Effect service layers by dynamically importing layer factories, resolving a merged layer, and running the Effect to completion.   |

---

## `src/routes/`

| File                       | Description                                                                                                                                                                                                                                                       |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `__root.tsx`               | Root layout route. Wraps every page with the document shell (head, CSS), Chakra UI `Provider`, devtools panels, `AuthClientContext`, `CommentsFnsContext`, nav bar, global shortcuts, and error/not-found boundaries. Pre-fetches the authenticated user session. |
| `-convert.machine.ts`      | XState v5 state machine for video conversion. Defines output formats, codec family detection, passthrough compatibility, and states for idle, file probing, ready, converting (WebCodecs with progress), success, and error.                                      |
| `-convert.machine.test.ts` | Tests for the video conversion state machine. Tests `getCodecFamily`, `isPassthroughCompatible`, and actor states for probing, conversion, and error handling.                                                                                                    |
| `account.tsx`              | `GET /account` — authenticated user settings page. Renders profile update form (name, avatar) and password change form using TanStack Form with Zod validation, plus delete account with confirmation dialog.                                                     |
| `convert.tsx`              | `GET /convert` — video conversion page route shell (thin file, lazy loads `convert.lazy.tsx`).                                                                                                                                                                    |
| `convert.lazy.tsx`         | Lazy-loaded video conversion page component. Upload a video, probe metadata, select output format, and download the converted result. Uses XState `convertMachine` with reactive selectors.                                                                       |
| `index.tsx`                | `GET /` — homepage. Shows "ViteSakuga" heading, a search box, and popular tags section with suspense fallback.                                                                                                                                                    |
| `upload.tsx`               | `GET /upload` — post upload page route shell (thin file, lazy loads `upload.lazy.tsx`).                                                                                                                                                                           |
| `upload.lazy.tsx`          | Lazy-loaded upload page component. File upload form with video preview, automatic thumbnail generation, tag input, metadata probing, and TanStack Form validation. Navigates to the new post on success.                                                          |
| `users.$id.tsx`            | `GET /users/$id` — user profile/posts page. Fetches posts by user with search/filter/pagination, renders user avatar/name plus `PostCard` grid in `PostsPageLayout`. Falls back to `NotFound` if user doesn't exist.                                              |
| `users.index.tsx`          | `GET /users/` — users index page. Fetches all users and renders them in a responsive grid of `User` cards with suspense fallback.                                                                                                                                 |
| `(auth)/login.tsx`         | `GET /login` — login page. Email/password form using `useLogin` mutation plus GitHub/Google social login buttons. Redirects after success using `redirectUrl` from auth layout.                                                                                   |
| `(auth)/route.tsx`         | Auth route group layout. In `beforeLoad`, redirects authenticated users to `/`. Provides `redirectUrl` context to child routes. Renders minimal `<Outlet />` with no extra chrome.                                                                                |
| `(auth)/signup.tsx`        | `GET /signup` — registration page. Four-field form (name, email, password, confirm password) with Zod validation via `useSignUp` mutation. Also supports social login.                                                                                            |
| `api/auth/$.ts`            | Catch-all API route: `GET / POST /api/auth/*`. Forwards all auth HTTP requests to Better Auth's handler (login, signup, session management, OAuth callbacks).                                                                                                     |
| `posts/$postId.tsx`        | `GET /posts/$postId` — post detail page. Fetches post, author, tags, and related post. Shows read-only view or edit form if current user is the owner. Includes error and not-found boundaries.                                                                   |
| `posts/index.tsx`          | `GET /posts/` — posts listing page. Renders `PostCard` grid with filters (sort, date range, tags, search), a spinner during fetch, empty state, and pagination. Shows debug info in development mode.                                                             |
| `posts/tags/$tag.tsx`      | `GET /posts/tags/$tag` — tag-filtered posts page. Fetches posts for a specific tag with sort/date range filters and pagination. SSR set to `"data-only"`.                                                                                                         |
