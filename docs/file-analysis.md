# File Analysis: `src/` Directory

Generated on 2026-08-17. Describes the purpose and contents of every file in `src/`.

---

## Root Files (`src/`)

| File                   | Description                                                                                                                                                                                                                                           |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/router.tsx`       | TanStack Router factory. Creates the app router with TanStack Query SSR integration, default catch boundary, 404 component, scroll restoration, and intent-based preloading. Also augments `@tanstack/react-router` module for type-safe route usage. |
| `src/routeTree.gen.ts` | Auto-generated route tree by TanStack Router. Imports all route modules, assembles the hierarchical route tree, and exports typed interfaces (`FileRoutesByFullPath`, `FileRoutesByTo`, etc.) plus the `routeTree` object. **Do not edit manually.**  |
| `src/vite-env.d.ts`    | Vite environment type declarations. Defines `import.meta.env` with `VITE_BASE_URL` and `VITE_CLOUDFLARE_R2_PUBLIC_URL` variables, and the `ImportMeta` interface.                                                                                     |

## `src/db/`

**Legacy migration folder.** Drizzle Kit migrations now live in the repo-root `drizzle/` directory (see `drizzle.config.ts` and `src/lib/db/e2e-db.ts`). `src/db/drizzle/` still contains only the original `0000_wandering_jimmy_woo.sql` migration with its `meta/` snapshot and journal; no code or tooling references it anymore.

## `src/styles/`

| File          | Description                                                                                                                                                                                                      |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app.css`     | Global CSS entry point. Imports Tailwind CSS v4, adds a class-based `dark` custom variant used by `next-themes`, and styles Ark UI toast group/root animations.                                                  |
| `safelist.ts` | Tailwind v4 safelist. Lists classes built dynamically by the Chakra-compat layer in `src/components/ui/` (spacing, grid columns, etc.) so they are not purged from the compiled CSS. Exports `tailwindSafelist`. |

## `src/utils/`

| File     | Description                                                                                                                                                                                 |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `seo.ts` | SEO meta tag generator. The `seo()` function accepts title, description, keywords, and image to return an array of tag objects (standard meta, Twitter Card, Open Graph) for `HeadContent`. |

---

## `src/components/`

### UI Primitives (`src/components/ui/`)

| File                     | Description                                                                                                                                                                                                        |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `button.tsx`             | Button primitive with `solid`/`outline`/`ghost`/`subtle` variants and blue/gray/red/green/orange palettes. Built on the Chakra-style props layer.                                                                  |
| `color-mode.tsx`         | Color mode provider and utilities. Wraps `next-themes`' `ThemeProvider`, exports `useColorMode`, `useColorModeValue`, `ColorModeIcon`, and `ColorModeButton` (toggle button).                                      |
| `feedback.tsx`           | Feedback primitives: `Badge`, `Spinner`, `Skeleton`, `Progress`, `Alert`, and `DataList` (Ark UI wrappers with Tailwind styling).                                                                                  |
| `field.tsx`              | Form field primitives: `Field` (Ark UI wrapper with Root/Label/HelperText/ErrorText), `Input`, `Textarea`, `InputGroup`, and `Checkbox`.                                                                           |
| `layout.tsx`             | Layout primitives: `Box`, `Flex`, `Center`, `Container`, `Stack`, `HStack`, `VStack`, `Grid`, `GridItem`, `SimpleGrid`, `Wrap`, `Group`, and `Span`.                                                               |
| `media.tsx`              | Media primitives: `Image`, `Avatar`, `AvatarGroup`, and `Card` (Ark UI avatar/card wrappers with Tailwind styling).                                                                                                |
| `overlay.tsx`            | Overlay/selection primitives wrapping Ark UI: `Dialog`, `Menu`, `Popover`, `Collapsible`, `Combobox`, `Select`, `Slider`, and `FileUpload` (plus `useCollapsibleContext` re-export).                               |
| `password-input.tsx`     | Password input with visibility toggle. Renders an `InputGroup` with an eye icon button to show/hide the password text.                                                                                             |
| `password-input.test.ts` | Unit tests for the password input visibility toggle.                                                                                                                                                               |
| `provider.tsx`           | Root UI provider. Wraps `next-themes`' `ThemeProvider`.                                                                                                                                                            |
| `tag-input.tsx`          | Tag selection/creation combobox. Allows selecting existing tags (fetched via query) or creating new ones, rendered as removable `Badge` elements.                                                                  |
| `toaster.tsx`            | Ark UI toast notification system. Renders an Ark `Toaster` in a portal with indicators, descriptions, and action buttons. Exports `Toaster` component and imperative `toaster` instance.                           |
| `typography.tsx`         | Typography primitives: `Text` (polymorphic `as` prop), `Heading`, and `Link`.                                                                                                                                      |
| `ui-utils.ts`            | Chakra-compat layer. Exports `cx` (clsx + tailwind-merge), `Slot` (Radix-style slot), and `useChakraProps`/`ChakraStyleProps` which map Chakra-like props (margin, padding, flex, grid, etc.) to Tailwind classes. |

### Form Components (`src/components/form/`)

| File            | Description                                                                                                                                                         |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `FieldInfo.tsx` | Form field error display for TanStack Form. Shows error messages in red when touched and invalid, or "Validating..." during validation.                             |
| `FieldText.tsx` | Reusable text/textarea field wrapper for Ark UI + TanStack Form. Renders label, required indicator, helper text, `Input` or `Textarea`, and `FieldInfo` for errors. |

### Post Detail Components (`src/components/PostDetail/`)

| File                    | Description                                                                                                                                             |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PostDetailDisplay.tsx` | Read-only post detail view. Renders a back button, the `Post` component in a bordered box, and the `Comments` section below.                            |
| `PostEditForm.tsx`      | Post edit form. Uses TanStack Form with title/textarea fields, `TagInput`, and save/cancel buttons. Uses `useBlocker` to prevent navigation when dirty. |

### Main Components (`src/components/`)

| File                          | Description                                                                                                                                                                                                          |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Comments.tsx`                | Comment system for posts. Renders a comment list with a submit textarea, each comment showing content, timestamp, and a delete button (with confirmation dialog). Uses suspense-fetched comments and mutation hooks. |
| `DefaultCatchBoundary.tsx`    | Top-level error boundary for TanStack Router. Renders an error display with "Try Again" and "Go Back"/"Home" buttons.                                                                                                |
| `GlobalShortcuts.tsx`         | Global keyboard shortcuts handler. Shows a `?` icon button (bottom-left) that opens the keyboard shortcuts dialog, plus hotkey bindings for `Shift+/`, `G+P`, `G+U`, `G+S` navigation sequences.                     |
| `KeyboardShortcutsDialog.tsx` | Modal dialog listing keyboard shortcuts. Displays key sequences as `Badge` elements in a `Table`, covering navigation and video frame seeking.                                                                       |
| `NotFound.tsx`                | 404 fallback component. Shows a message, "Go back" button, and "Start Over" link to `/`.                                                                                                                             |
| `Pagination.tsx`              | Pagination control. Renders previous/next chevron buttons, page number buttons with ellipsis gaps, and highlights the current page.                                                                                  |
| `PlaylistAddModal.tsx`        | Modal for adding/removing the current post from playlists, with inline playlist creation. Uses `PlaylistsFnsContext`, TanStack Query, and mutation feedback toasts.                                                  |
| `PopularTagsSection.tsx`      | Popular tags display. Shows a heading "Popular Tags" followed by `Badge` links (with post counts) to `/posts/tags/$tag`.                                                                                             |
| `Post.tsx`                    | Full post detail display. Renders video player (if `videoKey`), title, content, tag list, related post link, author card, `PostVoteButtons`, and an edit button for the post owner.                                  |
| `PostCard.tsx`                | Post card for list/grid views. Renders a 16:9 thumbnail image, title (2-line clamp), and content preview (1-line clamp) as a `Link` with hover effects.                                                              |
| `PostError.tsx`               | Post-specific error boundary. Delegates to `ErrorComponent` from TanStack Router.                                                                                                                                    |
| `PostFilters.tsx`             | Post filter bar. "Sort By" badges (Newest/Oldest) and "Date Range" badges (All Time, Today, This Week, This Month) that update URL search params via `useNavigate`.                                                  |
| `PostsPageLayout.tsx`         | Main layout for posts pages. Responsive grid with sidebar (search box, filters, popular tags) and main content area. Shows video metadata on post detail routes.                                                     |
| `PostVoteButtons.tsx`         | Like/dislike buttons with counts for a post. Uses `usePostVotes`/`useSetVote`; shows a "Login required" toast for anonymous users.                                                                                   |
| `SearchBox.tsx`               | Search input with tag combo-box. Renders a text input, tag selector with removable badges, and a search button. Uses debounced navigation.                                                                           |
| `User.tsx`                    | User profile card/link. Renders an `Avatar` and name inside a `Link` to `/users/$id`.                                                                                                                                |
| `UserError.tsx`               | User-specific error boundary. Delegates to `ErrorComponent` from TanStack Router.                                                                                                                                    |
| `Video.tsx`                   | Video player using `media-chrome`. Renders a `MediaController` with full custom controls (play/pause, mute, volume, seek, time display, fullscreen, playback rate) and frame-by-frame hotkeys.                       |
| `VirtualPostsGrid.tsx`        | Virtualized infinite-scroll post grid using `@tanstack/react-virtual`. Supports responsive column counts (`useResponsiveColumns`), prev/next page fetching with anchor index/scroll preservation, and spinner rows.  |

---

## `src/lib/`

### `src/lib/auth/`

| File                      | Description                                                                                                                                                                                                            |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `auth.hooks.ts`           | React Query mutation hooks wrapping Better Auth. Exports `useLogin`, `useSignUp`, `useUpdateProfile`, `useChangePassword`, `useDeleteAccount`, and `useSocialLogin` with query invalidation and navigation on success. |
| `auth.hooks.test.tsx`     | Tests for auth React Query hooks. Mocks Better Auth client, router, and toaster to verify correct client method calls and navigation/invalidation on success.                                                          |
| `auth.middleware.ts`      | Client-safe auth server function. Exports `getUserSession` (TanStack Start server fn) which dynamically imports and runs `getUserSessionEffect` from `session.effect.ts` with the middleware layer.                    |
| `auth.middleware.test.ts` | Tests for the `getUserSession` server function wrapper. Mocks `AuthService` and `RequestHeadersService` to verify session handling and null user return.                                                               |
| `auth.schemas.ts`         | Effect Schema validation schemas for auth forms: `loginSchema`, `signUpSchema` (with password match refinement), `profileSchema`, and `passwordSchema`.                                                                |
| `auth.schemas.test.ts`    | Tests for auth Effect Schema schemas. Validates correct data acceptance and error cases (invalid emails, short passwords, password mismatch, etc.).                                                                    |
| `client-context.tsx`      | React Context provider for the Better Auth client instance. Enables auth hooks to access the client without prop drilling.                                                                                             |
| `client.ts`               | Better Auth client initialization for the browser. Creates the singleton `authClient` via `createAuthClient` with the base URL from `VITE_BASE_URL`.                                                                   |
| `context.ts`              | Effect service definitions for server-side auth dependencies: `AuthSessionProvider`, `AuthService`, and `RequestHeadersService`.                                                                                       |
| `index.ts`                | Server-side Better Auth initialization. Creates the `auth` instance with Drizzle ORM PostgreSQL adapter, email/password auth, TanStack Start cookies plugin, and session caching.                                      |
| `session.effect.ts`       | Server-side session effects. Defines `SessionFetchError`, `getSessionEffect` (reads cookies, supports an e2e bypass, returns the Better Auth session) and `getUserSessionEffect` (returns `session.user ?? null`).     |

### `src/lib/assets/`

| File     | Description                                                                                                                                                 |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `url.ts` | Asset URL builder. Exports `assetUrl(key)` which joins an R2 object key with `VITE_CLOUDFLARE_R2_PUBLIC_URL` to generate fully qualified public asset URLs. |

### `src/lib/comments/`

| File                      | Description                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `comments.service.ts`     | Comments domain service and server functions. Defines `CommentsService` (Effect `Context.Service` with `make`), static accessors (`CommentsService.fetch`, `CommentsService.add`, `CommentsService.delete_`), `CommentsServiceLive` (layer), and TanStack Start server functions (`fetchComments`, `addComment`, `deleteComment`). Auth checks and ownership enforcement are inlined here. Schemas come from `src/lib/db/schema`. |
| `comments.fn.test.ts`     | Integration tests for comment server functions. Sets up in-memory Kysely database and tests fetch/add/delete with auth checks, ownership validation, and error handling.                                                                                                                                                                                                                                                          |
| `comments.fn-context.tsx` | React Context holding default comment server function implementations. Enables dependency injection and testing by swapping implementations.                                                                                                                                                                                                                                                                                      |
| `comments.hooks.ts`       | React Query mutation hooks for comments. Exports `useAddComment` and `useDeleteComment` with toast notifications and automatic query invalidation.                                                                                                                                                                                                                                                                                |
| `comments.hooks.test.tsx` | Tests for comment mutation hooks. Verifies correct payloads, query invalidation on success, and error toasts on failure.                                                                                                                                                                                                                                                                                                          |
| `comments.queries.ts`     | TanStack Query key factories and `queryOptions` for comments. Defines `commentsKeys` and `commentsQueryGetComments` (by post ID).                                                                                                                                                                                                                                                                                                 |

### `src/lib/db/`

| File                        | Description                                                                                                                                                                                                                                                                         |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `collections.ts`            | TanStack DB reactive collections. Defines `tagsCollection` and `usersCollection` (eager sync from server), plus `commentDraftsCollection` and `uploadDraftCollection` (localStorage persistence).                                                                                   |
| `context.ts`                | `KyselyDB` Effect service definition. A typed, dependency-injectable reference to the Kysely database layer via Effect's `Context.Service`.                                                                                                                                         |
| `e2e-db.ts`                 | E2E test database factory. Creates a PGlite in-memory instance, runs Drizzle migrations from the repo-root `drizzle/` folder against it, and returns a Kysely instance with `PGliteDialect`.                                                                                        |
| `kysely.ts`                 | Central Kysely setup. Converts Drizzle schema definitions to Kysely-compatible types via `Kyselify` and creates the singleton `kysely` instance with a Postgres dialect.                                                                                                            |
| `layer-factories.server.ts` | Async Effect Layer factories for server use. `makeDBLayer` provides `KyselyDB` + `StorageLive` + logging + tracing (selects PGlite when `DATABASE_DRIVER=pglite`); `makeAuthLayer` adds `AuthService` + `RequestHeadersService`; `makeMiddlewareLayer` provides auth services only. |
| `pglite-driver.ts`          | Custom Kysely dialect backed by `@electric-sql/pglite` (in-memory Postgres). Translates Kysely queries into PGlite's native interface for testing/local dev.                                                                                                                        |
| `pool.ts`                   | Database connection pooling. Uses a local `pg` `Pool` when `DATABASE_DRIVER=local`, otherwise a Neon Serverless `Pool`; exports a Drizzle instance (node-postgres or Neon HTTP) and `getKyselyPool()`.                                                                              |
| `test-utils.ts`             | Testing utilities for integration tests. Builds an in-memory PGlite-backed Kysely instance (migrated from root `drizzle/`) and provides `makeServiceTestLayer` with auth/headers/rustfs storage layers.                                                                             |

### `src/lib/db/schema/`

| File                   | Description                                                                                                                                                                                                                                            |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `auth.schema.ts`       | Drizzle ORM schema for Better Auth tables: `user`, `session`, `account`, and `verification`. Also defines Effect Schema insert/select schemas for the `user` table.                                                                                    |
| `sakuga.schema.ts`     | Drizzle ORM schema for application-domain tables: `tags`, `postTags` (junction), `posts`, `postVotes`, `playlists`, `playlistPosts` (junction), and `comments`, plus relations (one-to-many, many-to-one, self-referencing) and `commentInsertSchema`. |
| `sakuga.utils.ts`      | Effect Schema select/insert schemas for tags, posts, post votes, comments, playlists, and playlist posts. Exports `PostVote`, `PostWithVotes`, `DbSchemaSelect`, and `DbSchemaInsert` types.                                                           |
| `sakuga.utils.test.ts` | Unit tests for the schema definitions in `sakuga.utils.ts`.                                                                                                                                                                                            |
| `timestamp.ts`         | `TimestampSchema` — a `Schema.Union([Schema.Date, Schema.DateFromString])` so row validation works with both local `pg` (returns `Date`) and Neon/driver-returned timestamp strings.                                                                   |
| `index.ts`             | Barrel file re-exporting all schema exports from `auth.schema`, `sakuga.schema`, and `sakuga.utils`.                                                                                                                                                   |
| `seed.ts`              | Database seeding script. Uses `drizzle-seed` to populate the `posts` table with 150 rows. Runs only when `SEED_DB=true`.                                                                                                                               |

### `src/lib/effect/`

| File                   | Description                                                                                                                                                                                                                                  |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `effect.utils.ts`      | Effect wrapper around Kysely (`EffectKysely<DB>`). Adapts raw Kysely queries into `Effect.Effect`, provides `executeTakeFirstOption`, `executeTakeFirstOrError`, and a generator-based `transaction()` API. Defines `SqlError` tagged error. |
| `effect.utils.test.ts` | Tests for the `EffectKysely` wrapper helpers.                                                                                                                                                                                                |
| `logger.ts`            | Effect logging utility. Exports `withMinimumLogLevel` layer-composer and typed log level constants (`All`, `Fatal`, `Error`, `Warn`, `Info`, `Debug`, `Trace`, `None`).                                                                      |
| `schema.utils.ts`      | Schema decoding helpers used by server functions: `parse`, `parseStrict`, `safeParseStrict`, and `toStandardSchemaV1Strict`.                                                                                                                 |
| `tracing.ts`           | OpenTelemetry tracing integration. Exports `TracingLive` layer using `@effect/opentelemetry` `NodeSdk` with OTLP trace and log exporters for production observability.                                                                       |

### `src/lib/env/`

| File        | Description                                                                                                                                                                                                   |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `client.ts` | Client-side environment validation. Loads and validates `import.meta.env` variables (`VITE_BASE_URL`, etc.) using Effect's `Config`, validates `MODE` is `"development"` or `"production"`.                   |
| `defs.ts`   | Shared environment schemas and loaders without module-level side effects. App entry points (`env/server.ts`, `env/client.ts`) and the Drizzle CLI validate against the same definitions.                      |
| `server.ts` | Server-side environment validation. Loads and validates `process.env` variables (`DATABASE_URL`, `BETTER_AUTH_SECRET`, R2/GitHub OAuth keys, etc.), validates `BETTER_AUTH_SECRET` min length and `NODE_ENV`. |

### `src/lib/mutations/`

| File                         | Description                                                                                                                                                                                                    |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mutation-feedback.ts`       | Shared mutation feedback. Exports `useMutationWithFeedback` (wraps `useMutation` with success/error toasts and message fallback), plus `toastSuccess`, `toastError`, and `errorMessage` for specialized cases. |
| `mutation-feedback.test.tsx` | Tests for the mutation feedback helpers and hook.                                                                                                                                                              |

### `src/lib/pagination/`

| File            | Description                                                                                                                                                                                          |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pagination.ts` | Pagination utility. `computePagination(totalCount, { page, pageSize })` returns `PaginationMeta` with `currentPage` (1-indexed), `totalPages`, `hasMore`, `hasPrevious`, `offset`, `limit`, `total`. |

### `src/lib/playlists/`

| File                       | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `playlists.service.ts`     | `PlaylistsService` Effect service and server functions. Implements create/update/delete, add/remove/reorder posts, and fetch user playlists / playlist detail / playlists-for-post. Server functions: `createPlaylist`, `updatePlaylist`, `deletePlaylist`, `addPostToPlaylist`, `removePostFromPlaylist`, `reorderPlaylistPosts`, `fetchUserPlaylists`, `fetchPlaylistDetail`, `fetchPlaylistsForPost`. Ownership checks use `UnauthorizedError`/`ForbiddenError`. |
| `playlists.fn.test.ts`     | Integration tests for playlist server functions: CRUD, add/remove/reorder, visibility, and authorization.                                                                                                                                                                                                                                                                                                                                                           |
| `playlists.fn-context.tsx` | React Context holding default playlist server function implementations (`PlaylistsFnsContext`) for dependency injection/testing.                                                                                                                                                                                                                                                                                                                                    |
| `playlists.hooks.ts`       | React Query mutation hooks for playlists: `useCreatePlaylist`, `useUpdatePlaylist`, `useDeletePlaylist`, `useAddPostToPlaylist`, `useRemovePostFromPlaylist`.                                                                                                                                                                                                                                                                                                       |
| `playlists.queries.ts`     | TanStack Query key factories and `queryOptions` for playlists: `playlistsKeys`, `playlistQueryDetail`, `playlistsQueryForPost`, `playlistsQueryUserPlaylists`.                                                                                                                                                                                                                                                                                                      |
| `playlists.schema.ts`      | Effect Schema input schemas for playlist operations: create, update, add/remove post, reorder, and detail fetch.                                                                                                                                                                                                                                                                                                                                                    |

### `src/lib/posts/`

| File                      | Description                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `posts.service.ts`        | `PostsService` Effect service and server functions. Core business logic for `search` (q/tags/date-range/sort/paginate via Kysely, returns posts with vote counts), `fetchDetail` (with related post, comments, and vote counts), `upload` (R2 multipart with media analysis and thumbnail generation), `getByTag`, and `update`. Server functions (`searchPosts`, `fetchPostDetail`, `uploadPost`, `updatePost`) are embedded at the bottom of this file. |
| `posts.fn.test.ts`        | Integration tests for post server functions. Seeds test data and verifies search/filter/pagination, detail fetching with related posts, tag filtering, and update authorization.                                                                                                                                                                                                                                                                          |
| `posts.hooks.ts`          | React hooks for the virtualized posts grid. Exports `PostsInfiniteState` and `usePostsInfiniteScroll` which manage prev/next page fetching, anchor post index/scroll restoration, URL search param sync, and popular tags.                                                                                                                                                                                                                                |
| `posts.infinite.test.ts`  | Tests for the infinite-scroll hook state helpers.                                                                                                                                                                                                                                                                                                                                                                                                         |
| `posts.queries.ts`        | TanStack Query factories for the posts grid: `postsKeys`, `postsNextPageParam`/`postsPreviousPageParam`, `computeAnchorPostIndex`, `postsInfiniteQueryOptions`, and `postQueryDetail`.                                                                                                                                                                                                                                                                    |
| `posts.schema.ts`         | Effect Schema for post data: `VideoMetadataSchema` (codec, frame rate, resolution), `TagSchema`, `FormFileUploadTextSchema`/`FormFileUploadSchema`, `updatePostInputSchema`, `searchPostsBaseSchema` (q, tags, sortBy, dateRange `all                                                                                                                                                                                                                     | today | week | month`, page/pageSize), and `postByTagSchema`. |
| `posts.schema.test.ts`    | Tests for posts Effect Schema. Validates defaults, correct inputs, and error cases (negative pages, invalid sort options, unknown keys).                                                                                                                                                                                                                                                                                                                  |
| `useResponsiveColumns.ts` | Hook returning the active grid column count by mirroring the `SimpleGrid` breakpoints (5/4/3/2/1 columns) used by the posts grid.                                                                                                                                                                                                                                                                                                                         |

### `src/lib/storage/`

| File                | Description                                                                                                                                                                          |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `storage.module.ts` | `StorageModule` Effect service definition. Interface with `uploadVideo(userId, file)`, `uploadThumbnail(userId, file)`, `deleteFile(key)`. Tagged error: `StorageError`.             |
| `storage.s3.ts`     | S3/R2 implementation of `StorageModule`. Uses `@aws-sdk/client-s3` with Cloudflare R2 endpoint. Key generation: `{namespace}/{userId}/{uuid}.{ext}`. Provides `StorageLive` layer.   |
| `storage.rustfs.ts` | RustFS (S3-compatible, local) implementation of `StorageModule` for local dev/e2e tests. Points at `http://localhost:9000` with fixed credentials. Exports `makeRustFSStorageLayer`. |
| `storage.test.ts`   | Tests for `StorageModule` using the rustfs layer. Verifies uploadVideo/uploadThumbnail key formatting, content storage, and deleteFile.                                              |

### `src/lib/tags/`

| File              | Description                                                                                                                                                                                                                                               |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tags.service.ts` | `TagsService` Effect service and server functions. Implements `all` (fetch all tags) and `popular` (top 10 tags by post count via join). Server functions (`getAllTags`, `getAllPopularTags`) are embedded in this file. Exports `TagsServiceLive` layer. |
| `tags.fn.test.ts` | Integration tests for tag server functions. Tests empty/all tags retrieval and popular tags with correct post counts after seeding.                                                                                                                       |
| `tags.hooks.ts`   | React hook `useTagCollection` for the TanStack DB tags collection (create tag, look up by name).                                                                                                                                                          |
| `tags.queries.ts` | TanStack Query `queryOptions` factories for tags: `getTags` and `getPopularTags`. Defines `tagsKeys` and exports convenience functions.                                                                                                                   |
| `tags.utils.ts`   | Tag utility functions: `mapPopularTags` converts DB results (with bigint counts) to plain `{ id, name, postCount }` objects.                                                                                                                              |

### `src/lib/upload/`

| File                       | Description                                                                                                                                                                                                                                                                                              |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `upload.processor.ts`      | Client-side video file processing utilities. `makeReadChunk` for `mediainfo.js`, `analyzeVideo` for metadata extraction, `generateThumbnails` for frame capture, `generateAutoThumbnails` for 5 evenly-spaced auto thumbnails, and `buildFormData` for serialization.                                    |
| `upload.processor.test.ts` | Tests for upload processor. Tests `buildFormData` serialization (string/File/array/object, undefined skipping) and `makeReadChunk` chunk reading.                                                                                                                                                        |
| `useUploadDraft.ts`        | TanStack DB draft persistence hook. Provides `{ draft, isLoaded, persist, clear }` for auto-saving incomplete upload forms to localStorage.                                                                                                                                                              |
| `useUploadForm.ts`         | Upload form state hook. Manages the full upload workflow: video file, metadata, thumbnails, tags, and builds the FormData payload for submission.                                                                                                                                                        |
| `useVideoProcessing.ts`    | Video processing hook. Manages MediaInfo WASM lifecycle, video file selection, preview URL, thumbnail generation/capture, and frame rate state. Returns `{ videoFile, previewUrl, frameRate, thumbnails, selectedThumbnailIndex, videoMetadata, selectFile, captureFrame, selectThumbnail, clearFile }`. |

### `src/lib/users/`

| File               | Description                                                                                                                                                                                                              |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `users.service.ts` | `UsersService` Effect service and server functions. Implements `all` (fetch users) and `userPosts` (paged posts by user with popular tags). Server functions (`fetchUsers`, `fetchUserPosts`) are embedded in this file. |
| `users.fn.test.ts` | Integration tests for user server functions. Verifies user listing and user posts with pagination/filters.                                                                                                               |
| `users.queries.ts` | TanStack Query factories for users: `usersKeys` and `userPostsInfiniteQueryOptions`.                                                                                                                                     |
| `users.schema.ts`  | Effect Schema for users: `userPublicSchema` (`UserPublic`) and `fetchUserInputSchema` (`FetchUserInput`).                                                                                                                |

### `src/lib/votes/`

| File                   | Description                                                                                                                                                                                                                         |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `votes.service.ts`     | `PostVotesService` Effect service and server functions. Implements fetching vote summaries and setting/removing a like or dislike for a post (auth required). Server functions (`fetchPostVotes`, `setPostVote`, `removePostVote`). |
| `votes.fn.test.ts`     | Integration tests for vote server functions: counts, set/remove, idempotency, and auth requirements.                                                                                                                                |
| `votes.fn-context.tsx` | React Context holding default vote server function implementations (`VotesFnsContext`) for dependency injection/testing.                                                                                                            |
| `votes.hooks.ts`       | React Query hooks for votes: `usePostVotes` and `useSetVote` (passing `null` removes the vote). Exports `PostVotesSummary`.                                                                                                         |
| `votes.hooks.test.tsx` | Tests for vote hooks: payloads, query invalidation, and error toasts.                                                                                                                                                               |
| `votes.queries.ts`     | TanStack Query key factories for votes: `votesKeys` and `votesQueryGetVotes`.                                                                                                                                                       |
| `votes.schema.ts`      | Effect Schema input schemas: `setPostVoteSchema` and `removePostVoteSchema`.                                                                                                                                                        |
| `votes.utils.ts`       | Server-side helper `fetchPostVoteCounts` (batch vote counts per post) and the `VoteCountsMap` type.                                                                                                                                 |

### `src/lib/` (root)

| File                   | Description                                                                                                                                                                                                                                                    |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `errors.ts`            | Effect tagged error classes: `PostNotFoundError`, `UnauthorizedError`, `ForbiddenError`, `CommentNotFoundError`, `UserNotFoundError`, `ValidationError`, `PlaylistNotFoundError`, `PostAlreadyInPlaylistError`. Used for type-safe error handling in services. |
| `errors.test.ts`       | Unit tests for the tagged error classes.                                                                                                                                                                                                                       |
| `query-client.ts`      | Singleton TanStack Query client. `getQueryClient()` returns a lazily-initialized `QueryClient` with 60s default stale time.                                                                                                                                    |
| `sanitize.ts`          | HTML sanitization utility. The `sanitize` function strips `<script>`, `<iframe>`, `<object>`, `<embed>` tags, inline event handlers, and `javascript:` URLs from user input.                                                                                   |
| `server-fn.handler.ts` | `createHandler` factory. Bridges TanStack Start server functions with Effect service layers by dynamically importing layer factories (`baseLayerFactories.db`/`.auth`), resolving a merged layer, and running the Effect to completion.                        |

---

## `src/routes/`

| File                                  | Description                                                                                                                                                                                                                                                                                            |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `__root.tsx`                          | Root layout route. Wraps every page with the document shell (head, CSS), Ark UI `Provider`, devtools panels, `HotkeysProvider`, `AuthClientContext`, `CommentsFnsContext`, `PlaylistsFnsContext`, nav bar, and error/not-found boundaries. Pre-fetches the authenticated user session in `beforeLoad`. |
| `-convert.machine.ts`                 | XState state machine for video conversion. Defines output formats, codec family detection, passthrough compatibility, and states for idle, file probing, ready, converting (WebCodecs with progress), success, and error.                                                                              |
| `-convert.machine.test.ts`            | Tests for the video conversion state machine. Tests `getCodecFamily`, `isPassthroughCompatible`, and actor states for probing, conversion, and error handling.                                                                                                                                         |
| `account.tsx`                         | `GET /account` — authenticated user settings page. Renders profile update form (name, avatar) and password change form using TanStack Form with Effect Schema validation, plus delete account with confirmation dialog.                                                                                |
| `convert.tsx`                         | `GET /convert` — video conversion page route shell (thin file, lazy loads `convert.lazy.tsx`).                                                                                                                                                                                                         |
| `convert.lazy.tsx`                    | Lazy-loaded video conversion page component. Upload a video, probe metadata, select output format, and download the converted result. Uses XState `convertMachine` with reactive selectors.                                                                                                            |
| `index.tsx`                           | `GET /` — homepage. Shows "ViteSakuga" heading, a search box, and popular tags section with suspense fallback.                                                                                                                                                                                         |
| `upload.tsx`                          | `GET /upload` — post upload page route shell (thin file, lazy loads `upload.lazy.tsx`).                                                                                                                                                                                                                |
| `upload.lazy.tsx`                     | Lazy-loaded upload page component. File upload form with video preview, automatic thumbnail generation, tag input, metadata probing, and TanStack Form validation. Navigates to the new post on success.                                                                                               |
| `users.$id.tsx`                       | `GET /users/$id` — user profile/posts layout route. Renders user avatar/name plus the virtualized infinite-scroll `VirtualPostsGrid` in `PostsPageLayout`, with nested playlists routes. SSR set to `"data-only"`. Falls back to `NotFound` if user doesn't exist.                                     |
| `users.$id.playlists.index.tsx`       | `GET /users/$id/playlists` — user playlists index. Lists playlists (with thumbnails/post counts), creates new playlists, and renders edit/delete controls for the owner. SSR set to `"data-only"`.                                                                                                     |
| `users.$id.playlists.$playlistId.tsx` | `GET /users/$id/playlists/$playlistId` — playlist detail. Renders playlist info and a paginated post grid; owners can add/remove/reorder posts. SSR set to `"data-only"`.                                                                                                                              |
| `users.index.tsx`                     | `GET /users/` — users index page. Fetches all users via the TanStack DB `usersCollection` and renders them in a responsive grid of `User` cards with suspense fallback.                                                                                                                                |
| `(auth)/login.tsx`                    | `GET /login` — login page. Email/password form using `useLogin` mutation plus GitHub/Google social login buttons. Redirects after success using `redirectUrl` from auth layout.                                                                                                                        |
| `(auth)/route.tsx`                    | Auth route group layout. In `beforeLoad`, redirects authenticated users to `/`. Provides `redirectUrl` context to child routes. Renders minimal `<Outlet />` with no extra chrome.                                                                                                                     |
| `(auth)/signup.tsx`                   | `GET /signup` — registration page. Four-field form (name, email, password, confirm password) with Effect Schema validation via `useSignUp` mutation. Also supports social login.                                                                                                                       |
| `api/auth/$.ts`                       | Catch-all API route: `GET / POST /api/auth/*`. Forwards all auth HTTP requests to Better Auth's handler (login, signup, session management, OAuth callbacks).                                                                                                                                          |
| `posts/$postId.tsx`                   | `GET /posts/$postId` — post detail page. Fetches post, author, tags, and related post. Shows read-only view (with vote buttons) or edit form if current user is the owner. Includes error and not-found boundaries.                                                                                    |
| `posts/index.tsx`                     | `GET /posts/` — posts listing page. Renders `PostsPageLayout` with filters (sort, date range, tags, search) and the virtualized infinite-scroll `VirtualPostsGrid`. SSR set to `"data-only"`.                                                                                                          |
| `posts/tags/$tag.tsx`                 | `GET /posts/tags/$tag` — tag-filtered posts page. Fetches posts for a specific tag with sort/date range filters and the virtualized infinite-scroll grid. SSR set to `"data-only"`.                                                                                                                    |
